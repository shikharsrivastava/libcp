//19:55

#ifndef LIBCP_BIT
#define LIBCP_BIT

#include<bits/stdc++.h>
namespace libcp {
namespace ds {

template <typename T, typename F>
class Bit
{
	public:
	typedef int size_type;
	typedef F	func_type;
	// constructors
	Bit();
	Bit(size_type n);
	template <typename I>
	Bit(I s, I e, )

};



}
}

#endif
